# Installation of MkDocs

1. Go into the folder for your web application and run the following commands:

```bash
python3 -m venv venv
source venv/bin/activate
pip -install mkdocs-material
mkdocs new documentation
```

This will create a `documentation` folder in your project with the basic files.